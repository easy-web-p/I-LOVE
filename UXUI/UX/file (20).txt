Visitor opens URL
→ Server checks current time
→ Server compares revealAt and expiresAt
├── Before revealAt → Return countdown screen
├── Within valid period → Return website
└── After expiresAt → Return expired screen
